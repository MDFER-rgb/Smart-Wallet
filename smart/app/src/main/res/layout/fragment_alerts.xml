<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="#0D001A">

    <LinearLayout
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- COIN SELECT -->
        <Spinner
            android:id="@+id/spnCoin"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:background="@drawable/ios_input"
            android:popupBackground="#1A0D2E"
            android:padding="14dp"
            android:textColor="#FFFFFF" />

        <!-- OPERATOR -->
        <Spinner
            android:id="@+id/spnOperator"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_marginTop="14dp"
            android:background="@drawable/ios_input"
            android:popupBackground="#1A0D2E"
            android:padding="14dp"
            android:textColor="#FFFFFF" />

        <!-- TARGET PRICE -->
        <EditText
            android:id="@+id/edtTarget"
            android:hint="Target price (USD)"
            android:inputType="numberDecimal"
            android:textColor="#FFFFFF"
            android:textColorHint="#88FFFFFF"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_marginTop="14dp"
            android:background="@drawable/ios_input"
            android:padding="14dp" />

        <!-- ADD BUTTON -->
        <Button
            android:id="@+id/btnAddAlert"
            android:text="Add Alert"
            android:textAllCaps="false"
            android:textStyle="bold"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_marginTop="22dp"
            android:textColor="#0D001A"
            android:background="@drawable/ios_button" />

        <!-- TITLE -->
        <TextView
            android:id="@+id/txtYourAlerts"
            android:text="Your Alerts"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="18sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="25dp" />

        <!-- ALERT LIST -->
        <LinearLayout
            android:id="@+id/layoutAlertList"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:paddingBottom="60dp" />

    </LinearLayout>

</ScrollView>
